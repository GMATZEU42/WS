#include "time.h"

// #include <chrono>
// #include <thread>

namespace time
{
    timer::timer(time_interval t)
    {
        m_interval = t;
    };
    
    // ~timer::timer(){};

    void timer::start()
    {
        
    }

    void timer::stop()
    {
        
    }

}